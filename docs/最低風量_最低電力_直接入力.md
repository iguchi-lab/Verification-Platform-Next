# ユーザーマニュアル 最低風量・最低電力 直接入力

## 目次

- **Part 1: 関連する入力項目**
  - 入力一覧
  - 入力の組み合わせ

- **Part 2: 入力によるロジックの変化**
  - マクロ視点：計算全体における変更スコープ
  - ミクロ視点：ステップ2 の変更前後
  - ミクロ視点：ステップ4 の変更前後
    - Before（従来式）最低風量 直接入力: なし & 換気分を引く（既定値）
    - After 最低風量 直接入力: なし & 換気分を引かない
    - After 最低風量 直接入力: あり & 最低電力 直接入力: なし
    - After 最低風量 直接入力: あり & 最低電力 直接入力: あり

---

## Part 1: 関連する入力項目

下記は 暖房・冷房 別に設定されます。

### 入力一覧

| 入力名 | 入力型 | 入力値 | 内部値 | 既定値 | 変数名 |
|-------|--------|--------|-------|-------|--------|
| ファン消費電力から換気分を引くか | 選択 | 「換気分を引く」 / 「換気分を引かない」 | `1` / `2` | `1` | `subtract_ventilation_power` |
| 最低風量（入力しない or 入力する） | 選択 | 「入力しない」 / 「入力する」 | `1` / `2` | `1` | `input_V_hs_min` |
| 最低風量 [m³/h] | 数値 | 正の実数 [m³/h] | — | — | `V_hs_min` |
| 最低電力（入力しない or 入力する） | 選択 | 「入力しない」 / 「入力する」 | `1` / `2` | `1` | `input_E_E_fan_min` |
| ファン消費電力算定方法 | 選択 | 「直線近似法」 / 「風量三乗近似法」 | `1` / `2` | — | `E_E_fan_logic` |
| 最低電力 [W] | 数値 | 正の実数 [W] | — | — | `E_E_fan_min` |

**ファン消費電力算定方式の種類**

| 点 | 風量 x | 消費電力 y（直線近似法） | 消費電力 y（風量3乗近似法） |
|----|--------|--------------------------|-------------------------------|
| 最低点（直接入力） | `V_hs_min` | `E_E_fan_min` | `E_E_fan_min` |
| 算定点 | `V_hs_supply` | `E = a · V + b` | `E = a · V³ + b` |
| 定格点 | `V_hs_dsgn` | `P_fan_rtd` | `P_fan_rtd` |

### 入力の組み合わせ

```mermaid
flowchart TD

Start([入力設定の開始])
--> Q1{最低風量 直接入力<br/>input_V_hs_min}

Q1 -->|入力しない| A1[ファン消費電力から換気分を引くか<br/>subtract_ventilation_power]
A1 --> A1a["「換気分を引く」<br/>→ 内部値: 1"]
A1 --> A1b["「換気分を引かない」<br/>→ 内部値: 2"]

Q1 -->|入力する| B1["最低風量 V_hs_min [m³/h]<br/>入力必須"]
B1 --> Q2{最低電力 直接入力<br/>input_E_E_fan_min}

Q2 -->|入力しない| EndB([最低風量のみ反映])
EndB --> A1b
A1a & A1b --> EndA([消費電力計算は従来式])

Q2 -->|入力する| C1["最低電力 E_E_fan_min [W]<br/>必須"]
C1 --> C2["ファン消費電力算定方法 E_E_fan_logic<br/>必須"]
C2 --> C2a["「直線近似法」<br/>→ 内部値: 1"]
C2 --> C2b["「風量三乗近似法」<br/>→ 内部値: 2"]

style Start fill:#e1f5ff
style Q1 fill:#ffffcc
style A1 fill:#e1ffe1
style A1a fill:#e1f5ff
style A1b fill:#e1f5ff
style B1 fill:#ffe1cc
style Q2 fill:#ffffcc
style EndB fill:#e1f5ff
style EndA fill:#e1f5ff
style C1 fill:#ffe1cc
style C2 fill:#ffe1cc
style C2a fill:#ffe1cc
style C2b fill:#ffe1cc
```

---

## Part 2: 入力によるロジックの変化

### マクロ視点: 計算全体における変更スコープ

共通のフローである [計算フロー タイプ1・2](計算フロー_タイプ1・2.md) の各ステップにおける影響範囲を示します。

**凡例：**

| 色 | 意味 |
|---|------|
| 🟩 | 変更なし（従来ロジックのまま） |
| 🟨 | 間接的に影響あり |
| 🟧 | 直接的に影響あり（本機能のメイン変更箇所） |

```mermaid
flowchart LR

Step1[ステップ1<br/>送風機設計風量の決定]
Step2[ステップ2 ※1<br/>未処理負荷と<br/>関連温湿度・風量<br/>calc_Q_UT_A]
Step3[ステップ3<br/>熱源機平均能力の計算]
Step4[ステップ4 ※2<br/>送風機消費電力の計算]
Step5[ステップ5<br/>消費電力の計算]

Step1 --> Step2 --> Step3 --> Step4 --> Step5

style Step2 fill:#fff3cd
style Step4 fill:#ffe1cc
style Step1 fill:#e1ffe1
style Step3 fill:#e1ffe1
style Step5 fill:#e1ffe1
```

**注釈:**
- ※1 最低風量 直接入力:あり なら給気風量の下限に適用
- ※2 最低電力 直接入力:あり ならカスタムロジックを使用

| ステップ | 変更条件 | 影響 | 内容 |
|---------|---------|------|------|
| ステップ 1 | — | なし | 設計風量 V_hs_dsgn は変わらない |
| ステップ 2 | 最低風量 直接入力:あり | **あり** | 各区画の全般換気量を合計が V_hs_min になるように決定 |
| ステップ 3 | — | なし | 熱源機能力の計算は変わらない |
| ステップ 4 | 最低電力 直接入力:あり | **あり** | V_hs_vent の下限適用・消費電力算定式の切替 |
| ステップ 5 | — | なし | 圧縮機電力の計算は変わらない |

---

### ミクロ視点: ステップ2 の変更前後

ステップ2 (`calc_Q_UT_A`) では、各区画の換気風量 `V_vent_g_i` を集計して `V_hs_vent` を決定する。
最低風量 直接入力:あり の場合、 `V_vent_g_i` の合計が `V_hs_min` がとなるように `V_vent_g_i` が決定する。

#### Before（従来式）

```mermaid
flowchart LR

Vg["標準住戸の全般換気量と室面積から算出<br/>V_vent_g_i"]

Vg --> OutSum(["V_hs_vent = Σ V_vent_g_i"])

style Vg fill:#e1ffe1
style OutSum fill:#e1ffe1
```

---

#### After: 最低風量 直接入力: あり

```mermaid
flowchart LR

Vmin["V_hs_min ★<br/>最低風量 直接入力"]
Vg["従来 V_vent_g_i"]

Vg & Vmin --> Calc["V_vent_g_i = 従来V_vent_g_i・V_hs_min / Σ V_vent_g_i"]

Calc --> OutSum(["V_hs_vent = Σ V_vent_g_i"])

style Vg fill:#e1ffe1
style Vmin fill:#ffe1cc
style Calc fill:#ffe1cc
style OutSum fill:#e1ffe1
```

---

### ミクロ視点: ステップ4 の変更前後

#### Before（従来式）最低風量 直接入力: なし & 換気分を引く（既定値）

```mermaid
flowchart LR

subgraph 入力
    V(["V_hs_supply<br/>給気風量"])
    V_dsgn(["V_hs_dsgn<br/>設計風量"])
    V_vent(["V_hs_vent<br/>換気風量<br/>下限考慮なし"])
    P(["P_fan_rtd<br/>定格消費電力"])
end

V & V_dsgn & V_vent --> Ratio["換気補正風量比<br/>(V_hs_supply - V_hs_vent) / (V_hs_dsgn - V_hs_vent)"]

Ratio & P & V_vent --> Power["消費電力計算<br/>(P_fan_rtd - f_SFP・V_hs_vent)・ratio</br>+ f_SFP・V_hs_vent"]

Power --> SVPCheck{換気分を引く?}
SVPCheck -->|引く| Check{サーモON ?<br/>q_hs ＞ 0}
Check -->|YES| Result(["E_E_fan"])
Check -->|NO| Zero([0])

style V fill:#e1f5ff
style V_dsgn fill:#e1f5ff
style V_vent fill:#e1f5ff
style P fill:#e1f5ff
style Ratio fill:#e1ffe1
style Power fill:#e1ffe1
style SVPCheck fill:#ffffcc
style Check fill:#ffffcc
style Result fill:#e1f5ff
style Zero fill:#e1f5ff
```

---

#### After 最低風量 直接入力: なし & 換気分を引かない

**最低風量・最低電力の直接入力なしで、「換気分を引かない」を選択した場合。**

ファン消費電力から換気分を引くか:
- 換気分を引く（従来式）
    - `P_fan_rtd` から換気分 `f_SFP・V_hs_vent` を差し引いた上で風量比を掛けている。
- 換気分を引かない
    - 換気分のファン消費電力 `P_fan_vent = f_SFP・V_hs_vent` を差し引かず、消費電力に含める。
    - 結果として、ファンの全消費電力（空調分 + 換気分）が暖冷房設備の消費電力として計上される。
    - よって、サーモOFF (`q_hs ≦ 0`) でも電力が 0 にならず、**暖冷房期間中は常にファン消費電力を計上**される。
      - 暖房: 暖房期間のみ計上
      - 冷房: 冷房期間 + 中間期に計上

```mermaid
flowchart LR

subgraph 入力
    V(["V_hs_supply<br/>給気風量"])
    V_dsgn(["V_hs_dsgn<br/>設計風量"])
    V_vent(["V_hs_vent<br/>換気風量<br/>下限考慮なし"])
    P(["P_fan_rtd<br/>定格消費電力"])
end

V & V_dsgn & V_vent --> Ratio["換気補正風量比<br/>(V_hs_supply - V_hs_vent) / (V_hs_dsgn - V_hs_vent)"]

Ratio & P & V_vent --> Power["消費電力計算<br/>(P_fan_rtd - f_SFP・V_hs_vent)・ratio</br>+ f_SFP・V_hs_vent"]

Power --> SVPCheck{換気分を引く?}
SVPCheck -->|引かない| Check2{暖房 or 冷房 ※1}
Check2 --> |暖房| Check3{中間期 ? ※2}
Check2 --> |冷房| Result(["E_E_fan"])

Check3 -->|NO| Result
Check3 -->|YES| Zero([0])

style V fill:#e1f5ff
style V_dsgn fill:#e1f5ff
style V_vent fill:#e1f5ff
style P fill:#e1f5ff
style Ratio fill:#e1ffe1
style Power fill:#ffe1cc
style SVPCheck fill:#ffffcc
style Check2 fill:#ffffcc
style Check3 fill:#ffffcc
style Result fill:#e1f5ff
style Zero fill:#e1f5ff
```

**注釈:**
- ※1 サーモOFFであってもファン消費電力を計上する
- ※2 合算した時重複しないよう、暖房では中間期を計上しない

---

#### After 最低風量 直接入力: あり & 最低電力 直接入力: なし

**最低風量のみ指定。計算ロジックは 2.3.2 と同じだが、V_hs_vent が Step 2 で既に上書き済み。**
V_hs_vent = max(V_hs_min, Σ V_vent_g_i) または V_hs_min

```mermaid
flowchart LR

subgraph 入力
    V(["V_hs_supply<br/>給気風量"])
    V_dsgn(["V_hs_dsgn<br/>設計風量"])
    V_vent(["V_hs_vent ★<br/>換気風量<br/>下限考慮あり"])
    P(["P_fan_rtd<br/>定格消費電力"])
end

V & V_dsgn & V_vent --> Ratio["換気補正風量比<br/>(V_hs_supply - V_hs_vent) / (V_hs_dsgn - V_hs_vent)"]

Ratio & P & V_vent --> Power["消費電力計算<br/>(P_fan_rtd - f_SFP・V_hs_vent)・ratio</br>+ f_SFP・V_hs_vent"]

Power --> SVPCheck{換気分を引く?}
SVPCheck -->|引かない| Check2{暖房 or 冷房 ※1}
Check2 --> |暖房| Check3{中間期 ? ※2}
Check2 --> |冷房| Result(["E_E_fan"])

Check3 -->|NO| Result
Check3 -->|YES| Zero([0])

style V fill:#e1f5ff
style V_dsgn fill:#e1f5ff
style V_vent fill:#ffe1cc
style P fill:#e1f5ff
style Ratio fill:#e1ffe1
style Power fill:#ffe1cc
style SVPCheck fill:#ffffcc
style Check2 fill:#ffffcc
style Check3 fill:#ffffcc
style Result fill:#e1f5ff
style Zero fill:#e1f5ff
```

**注釈:**
- ※1 サーモOFFであってもファン消費電力を計上する
- ※2 合算した時重複しないよう、暖房では中間期を計上しない

---

#### After 最低風量 直接入力: あり & 最低電力 直接入力: あり

**最低風量 + 最低電力を指定。カスタム消費電力式を使用。従来式とは異なり、サーモOFF時も電力を計算する（0にならない）。**
V_hs_vent = max(V_hs_min, Σ V_vent_g_i) または V_hs_min

```mermaid
flowchart LR

subgraph 入力パラメータ
    V([V_hs_supply<br/>給気風量])
    V_vent([V_hs_vent ★<br/>換気風量<br/>下限考慮あり])
    V_dsgn([V_hs_dsgn<br/>設計風量])
    P([P_fan_rtd<br/>定格消費電力])
end

subgraph 追加入力パラメータ
    E_min([E_E_fan_min ★<br/>最低電力 直接入力])
    Logic([E_E_fan_logic ★<br/>算定方式])
end

V & V_vent --> FlowDecision{サーモOFF?<br/>q_hs ≦ 0}

FlowDecision -->|OFF| UseMin["使用風量 = V_hs_vent"]
FlowDecision -->|ON| UseActual["使用風量 = max(V_hs_supply, V_hs_vent)"]

Logic --> FormulaSelect{算定方式選択<br/>E_E_fan_logic}

FormulaSelect -->|直線近似法| LinearFormula["直線近似式<br/>E = a×V + b"]
FormulaSelect -->|風量三乗近似法| CubicFormula["風量3乗近似式<br/>E = a×V³ + b"]

LinearFormula & CubicFormula --> PowerCalc["係数計算 & 電力算出<br/>2点: (V_hs_vent, E_min), (V_dsgn, P_rtd)"]

UseMin & UseActual & V_vent & E_min & P & V_dsgn --> PowerCalc

PowerCalc --> SVPCheck{換気分を引く?}
SVPCheck -->|引かない| Check2{暖房 or 冷房}
Check2 -->|暖房| Check3{中間期 ? ※}
Check2 -->|冷房| Result([E_E_fan])

Check3 -->|NO| Result
Check3 -->|YES| Zero([0])

style V fill:#e1f5ff
style V_vent fill:#ffe1cc
style V_dsgn fill:#e1f5ff
style P fill:#e1f5ff
style E_min fill:#ffe1cc
style Logic fill:#ffe1cc
style FlowDecision fill:#ffffcc
style UseMin fill:#e1ffe1
style UseActual fill:#e1ffe1
style FormulaSelect fill:#ffe1cc
style LinearFormula fill:#ffe1cc
style CubicFormula fill:#ffe1cc
style PowerCalc fill:#ffe1cc
style SVPCheck fill:#ffffcc
style Check2 fill:#ffffcc
style Check3 fill:#ffffcc
style Result fill:#e1f5ff
style Zero fill:#e1f5ff
```

**注釈:**
- ※ 合算した時重複しないよう、暖房では中間期を計上しない

---

## 更新履歴

- 2026-03-12: 初版作成
- 2026-03-17: 換気分を引かない場合について追記
