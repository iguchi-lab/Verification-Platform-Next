# Changelog

このプロジェクトの利用者に影響する変更を記録します。版番号は
[Semantic Versioning](https://semver.org/) に従います。

## Unreleased

## [1.3.0] - 2026-08-14

### Changed

- 入力画面を目的別の8セクションへ再編し、暖房・冷房は選択方式の項目を同じ
  セクション内に表示するようにした。建研Web由来項目の表示名を建研Webの文言へ
  揃え、各セクション・グループ・主要入力に用途と計算への影響を説明する文を追加した。
- 入力画面ガイドを追加し、床下関連設定の入力ガイドを新しい画面構成へ更新した。

## [1.2.0] - 2026-08-13

### Added

- 計算完了後に、暖房・冷房別の一次エネルギー消費量、未処理負荷、エアコン消費電力量、
  ファン消費電力量を年間値と単位付きのカードで表示するようにした。

### Changed

- 入力画面を開いた直後は、①・②を含むすべての入力セクションを閉じた状態にした。
- 計算ログを、計算条件、実行処理、年間結果、出力ファイル、計算エンジン詳細ログの
  5区分に整理した。再計算開始時には前回の年間結果を消去し、完了後に新しい結果へ
  更新するようにした。

## [1.1.2] - 2026-08-12

### Changed

- 床下空調の入力項目を実際の計算経路に合わせて整理した。`R_g`と`φ`は手動設定を維持し、
  不易層温度は外気温の年間平均から自動算定、床板熱貫流率は用途別の固定・算定値として
  説明表示へ変更した。入力ガイドも現行UIと入力仕様`260812`に整合させた。
- システムCOPグラフへ、年間処理熱量を熱源機・送風機の年間消費電力量で除した年間APFを
  表示し、暖房APFと冷房APFも併記するようにした。
- READMEの冒頭に、公開中のCloud Run版を直接開けるリンクと利用上の注意を追加した。

## [1.1.1] - 2026-08-11

### Fixed

- RAC活用型全館空調の冷房SHFを、負荷バランス前のRAC負荷ではなく、
  負荷バランス後の全館空調負荷から算定するよう修正した。RAC特性で求める
  最大冷房能力は維持し、その顕熱・潜熱配分だけを全館空調負荷に整合させた。
- 選択を変更しても残っていたダクト式セントラル空調機とルームエアコンの
  条件付き入力欄を、現在の選択に合わせて即時に表示・非表示するよう修正した。
- 条件付き入力がすべて非表示のときに、「手動設定する補助定数」などの
  空のグループ見出しだけが残らないよう修正した。

### Changed

- 入力を既定値へ戻すボタンと、既定値から変更した入力を示す緑色表示を追加した。
  建研Web由来の青色とVerification Platform拡張の橙色は維持した。
- 過剰熱量持越し、最低風量・最低電力、機器能力・仕様などの従属入力を、
  対応する方式を選択した場合だけ表示するよう整理した。
- 計算時定数、床下空調、暖房・冷房全般の入力を責務別の小見出しへ再分類し、
  風量式と熱源機出口温度の設定説明を補足した。

## [1.1.0] - 2026-08-09

### Changed

- Web版の計算を1件ずつ処理する共通キューへ統一し、待機中であることを画面に
  明示するようにした。各計算へ計算IDを付けて専用ディレクトリへ成果物を出力し、
  同じケース名による上書きを防止した。Cloud Runは当面最大1インスタンスとする。
- 旧NotebookのPythonフォーム評価器を公開APIと通常の入力生成経路から分離し、
  Phase 5の旧版照合だけに使う非公開回帰ユーティリティへ限定した。
- 入力画面の大見出し（①・②～⑨）と、その内側の小見出しの文字サイズ・太さを
  調整し、入力階層を判別しやすくした。アコーディオン内の1px端数による
  不要な横スクロールバーも表示しないようにした。

## [1.0.2] - 2026-08-09

### Changed

- 入力画面の「① 計算条件名」と「② 外部ファイル名」を
  「①・② 計算条件・外部ファイル」へ統合し、「⑥-1 過剰熱量持越し」を
  「⑥ その他」内の独立グループへ統合した。
- 暖房・冷房の設備方式別セクションは、選択中の方式だけを表示するようにした。
- 入力グループの小見出しを大きくし、「⑨ 熱交換型換気設備）」の不要な末尾括弧を
  削除した。
- ルームエアコン活用型のエネルギー消費効率区分を、建研Webと同じく暖房・冷房の
  両カテゴリーに設け、それぞれの定格効率を独立して算定できるようにした。
  暖房側入力を持たない旧JSONは、従来どおり冷房効率から暖房効率を換算する。
- 現在の入力画面の配置は維持したまま、建研Webにある入力を青、
  Verification Platformで追加・拡張した入力を橙で識別できるようにした。

### 文書

- 建研本家の不具合候補、旧Verification Platform、Verification Platform Nextの
  計算差を区別する差分台帳を追加しました。

### 追加

- 冷房時の間仕切熱移動と、全般換気なし時の給気風量下限について、既定OFFで
  建研本家互換、チェック時だけ物理的な補正式を使う入力項目を追加しました。
  間仕切熱移動の補正は非床下、過剰熱量持越し、新床下の全経路へ適用されます。

## [1.0.1] - 2026-08-05

### 修正

- Colab の Gradio 公開URLで共有トンネルが上流へ到達できず504になる問題を軽減するため、
  Colabでは `0.0.0.0` 待受、デバッグ表示、1秒間隔のキュー状態更新を使用するようにしました。
  あわせてGradioを検証済みの `6.20.0` に固定しました。
- Gradioの計算とグラフ生成を分離し、計算中の状態を直ちに表示するよう変更。
  計算完了時点でログとCSVを先に返すため、グラフ生成中に接続が切れても
  計算結果を確認しやすくしました。
- 全般換気なしのダクト式セントラル空調で、中間期の送風機電力が残る問題と、
  暖冷房期のサーモOFF風量が設備最低風量と一致しない問題を修正しました。
- `VAVあり × 全般換気なし` の小負荷・サーモOFF時も設備最低風量を保証し、
  最低電力の補間点を全般換気量ではなく設備最低風量へ修正しました。
- 最低風量、設計風量、最低電力、定格ファン電力の矛盾した入力を明示的に
  エラーとして検出するようにしました。
- GitHub上で床下計算文書の数式が壊れて表示される問題を修正しました。

### 文書・操作性

- 床下関連の入力項目について、設定間の関係、影響範囲、推奨値、排他条件を
  入力ガイドへ整理しました。
- 風量設定、VAV、全般換気、最低風量、最低電力の計算ルートを、表とフロー図で
  文書化しました。本番運用中の既存JJJへ修正を移植するためのメモも追加しました。
- Notebookに、全セルの実行、起動セルの役割、ローカルURLと公開URLの使い分けを
  追記しました。
- 公開リポジトリ向けに内輪の運用メモと不要な作業用設定を整理しました。

## [1.0.0] - 2026-07-26

最初の正式リリースです。表示名は `ver.1.0.0`、Gitタグは `v1.0.0` です。

### 主な内容

- 入力スキーマ、Gradio UI、計算エンジンを単一モノレポへ統合
- 旧版との代表2ケース・8760時間系列のPhase 5数値回帰を固定
- 建築研究所公式`pyhees` Ver.3.10を取り込み、上流更新手順を整備
- `pyhees -> jjjexperiment`の逆依存とNext刷新前の互換shimを削除
- 長い計算調整役を責務別の境界へ分離し、建研式番号・変数名・評価順を維持
- 床下計算を「床下14」仕様へ整合し、根拠・数式・Excel異常レビューを文書化
- 床下計算7変更点を、元式・採用式・物理的意味とともに説明する対外文書を追加
- 古い期待失敗・skipを通常試験へ戻し、計算エンジンの警告源を整理
- 成果物名を`_v1.0.0`へ統一し、版、Gitコミット、上流pyhees版、
  床下仕様、入力SHA-256をJSONマニフェストへ記録

### 互換性

- Next刷新前の旧import互換shimは提供しません。
- 成果物の従来の日付接尾辞（例: `_20260413`）は`_v1.0.0`へ変わります。
- 計算式を意図せず変更するリリースではありません。数値一致はPhase 5で検証します。
